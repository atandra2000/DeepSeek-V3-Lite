torch>=2.4.0
transformers>=4.44.0
datasets>=2.20.0
huggingface_hub>=0.24.0
safetensors>=0.4.3
tqdm>=4.66.0
numpy>=1.26.0
matplotlib>=3.9.0
pyyaml>=6.0.1

# Test-only dependencies (kept here so `pip install -r requirements.txt` is
# enough for CI; not imported by training/inference code).
pytest>=8.0.0
pytest-cov>=4.0.0
